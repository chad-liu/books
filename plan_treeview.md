# plan_treeview.md — 檔案樹瀏覽器

## 原始需求

1. 做一個網頁瀏覽網站 treeview.html
2. 左邊欄為 books\html\ 的樹狀結構檔案選單，右邊欄為 html 瀏覽
3. 右邊欄有 tab，可同時開啟多個檔案
4. 有 html 編輯功能
5. 右邊欄有字型大小增減和切換 light/dark

## 已確認的設計決定

- **使用場景**：本機 localhost 與 file:// 直開**都要支援**，依環境自動偵測降級。
- **編輯模式**：**原始碼編輯**（textarea + 即時預覽）。不做視覺化所見即所得——這些頁面
  大量使用手工計算座標的 SVG，`contentEditable` 會重寫 DOM 並破壞屬性與精細排版。

## 一、架構

### 檔案

| 檔案 | 角色 |
|---|---|
| `tools/build_treeview.py` | 掃描 `html\`，把檔案樹烘進 `treeview.html` |
| `treeview.html` | 應用本體，放在 repo 根目錄（與 `index.html` 同層，路徑才是 `html/...`） |

### 為什麼檔案樹要「烘進去」而不是執行時抓

三種環境沒有一種能在執行時列出目錄：GitHub Pages 沒有目錄列表 API、`file://` 會被 CORS
擋掉、`python -m http.server` 雖有目錄列表但格式不保證。**建置時把樹烘成 JS 物件是唯一
三種環境都能用的做法**，也與既有的 `build_html_index.py` 一致（同樣是掃描後產生靜態檔）。

新增或改名檔案後重跑 `python tools/build_treeview.py` 即可，跟 index.html 的維護方式相同。

### 檔案樹的排序與內容

- 資料夾：依名稱升冪（與 `index.html` 相同，資料夾名已用數字前綴人工排好）。
- 檔案：**先依該資料夾 `order.md` 的順序**，`order.md` 未列到的接在後面依檔名排序。
- **未列入 `order.md` 的檔案加一個淡色標記**。這與 `index.html` 只顯示 order.md 條目
  的行為刻意不同——treeview 是檔案瀏覽器，看得到全部檔案才有維護價值（先前
  `荷馬史詩人物關係圖.html` 就是漏列了很久沒被發現）。

## 二、版面

```
┌───────────────────────────────────────────────────────┐
│ 標題 │ 搜尋框 │ A− A+ │ ☀/🌙 │ 環境狀態指示             │
├──────────────┬────────────────────────────────────────┤
│ 左欄（可收合）│ 分頁列 [檔A ×] [檔B ×] [檔C ×]          │
│              ├────────────────────────────────────────┤
│ ▼ 1.西方歷史  │ ○ 檢視  ○ 編輯          [存檔] [下載]   │
│   啟蒙運動    ├──────────────────┬─────────────────────┤
│   宗教改革    │                  │                     │
│   ⋯          │   原始碼          │   即時預覽           │
│ ▶ 2.中國歷史  │   textarea       │   iframe(srcdoc)    │
│ ▶ 3-0.宗教   │                  │                     │
│ ⋯            │                  │                     │
└──────────────┴──────────────────┴─────────────────────┘
```

- 左欄可收合，寬度可拖曳調整。
- 檢視模式時右欄整片給 iframe；編輯模式才切成左右分割。

## 三、分頁

- 每個分頁保存 `{path, mode, dirty, draft, scrollTop}`。
- iframe **延遲載入**：切換到該分頁才真正載入，避免一次開多個大檔。
- 分頁有未存檔變更時標題顯示 `●`，關閉前確認。
- 中鍵點擊分頁 = 關閉；Ctrl+W 關閉當前分頁。

## 四、編輯與存檔

### 讀取

`fetch(path)` 取得原始碼填入 textarea。

### 即時預覽

textarea 變更後 debounce 約 400ms，把內容丟進 `iframe.srcdoc`。
`srcdoc` 的相對路徑基準與父頁不同，需在預覽內容注入 `<base href="html/該資料夾/">`
才能讓頁面內的相對連結正常運作。

### 存檔的三種路徑（依環境自動選擇）

| 方式 | 條件 | 行為 |
|---|---|---|
| **直接存回原檔** | Chromium 系 + 安全內容（localhost/https） | File System Access API，第一次要求授權該檔案，之後 Ctrl+S 直接寫回 |
| **下載** | 任何能讀到原始碼的環境 | 下載修改後的檔案，使用者自行覆蓋 |
| **草稿保留** | 全部環境 | `localStorage` 以路徑為鍵自動存草稿，關掉瀏覽器不會遺失 |

### 環境降級對照

| 環境 | 瀏覽／分頁 | 讀原始碼 | 存回原檔 | 下載 |
|---|---|---|---|---|
| localhost（Chrome／Edge） | ✓ | ✓ | ✓ | ✓ |
| localhost（Firefox／Safari） | ✓ | ✓ | ✗ | ✓ |
| GitHub Pages | ✓ | ✓ | ✗ | ✓ |
| `file://` 直開 | ✓ | **✗** | ✗ | ✗ |

`file://` 下瀏覽器會擋住 `fetch` 讀取本機檔案，因此**編輯功能自動關閉**，只保留瀏覽與
分頁，並在頂部顯示一行提示說明「要編輯請改用 localhost 開啟」。這是瀏覽器的安全限制，
不是可以繞過的實作問題。

## 五、縮放與 light/dark

### `A−` / `A+` 縮放的是右邊預覽欄

**不能只改 font-size。** 這些頁面的字級多半寫成絕對 px，而 SVG 內的 `font-size` 屬於
SVG 使用者單位、不隨 CSS 字級繼承——只改字級的結果會是 HTML 內文放大、但圖表完全不動，
版面反而更亂。因此改用 `zoom` 對整份文件等比縮放。

作法：預覽用的 iframe 與主頁同源，可直接取 `iframe.contentDocument` 注入
`documentElement.style.zoom`。範圍 50%–250%，每次 10%，比例顯示在按鈕之間，
並記在 `localStorage`；換檔或換分頁後由 iframe 的 `onload` 自動重新套用。

`file://` 之下取不到 iframe 內容，此時按鈕自動停用並在提示中說明原因。

左欄樹狀選單與原始碼編輯器維持固定 13px，不受這兩顆按鈕影響。

### 為什麼沒有 dark/light 切換

原始需求第 5 點有這一項，實作後**移除**。要切換的對象是右邊預覽欄，但這件事做不好：

| 證據 | 數字 |
|---|---|
| 已轉淺色的頁面 | 92 個 |
| 這些頁面 SVG 內**寫死**的顏色 | 12,588 處 |
| SVG 內使用 CSS 變數的 | **0 處** |

SVG 不吃 CSS 變數，所以頁面的圖表顏色一律寫死（這也是 infograph-style skill 明載的規則）。
兩條可能的路都不通：

1. **從外部覆寫 CSS 變數**——只會把頁面外框變深，12,588 處圖表顏色原封不動，
   變成深色頁面上浮著一堆亮白圖表，比兩種單純狀態都糟。
2. **`filter: invert()`**——會機械式反轉那 12,588 個色值，深藍頁首變米色，
   並摧毀 brass／steel／copper／verde 這套用來區分類別的語意配色。

第三條路（在瀏覽器裡跑一次反向的重新著色演算法）等於把 skill 的整套轉換邏輯搬進檢視器，
既脆弱又是在對抗剛完成的設計工作，不值得。

因此 treeview 介面固定為淺色，與它所顯示的頁面一致。

## 六、其他行為

- **預設全部收合**：11 個資料夾一開始都是收合狀態，只顯示資料夾名與檔案數，一眼看完全貌
  再決定展開哪一個。手動展開的狀態會記在 `localStorage`，下次開啟維持不變。
- **搜尋**：即時過濾樹狀選單，比對檔名與資料夾名，命中項目自動展開所屬資料夾；
  清空搜尋後回到使用者原本的展開狀態，不會停在被搜尋撐開的樣子。
- **大檔保護**：荷馬史詩兩本各約 9 MB。瀏覽沒問題，但載進 textarea 會嚴重卡頓。
  設 2 MB 門檻，超過者預設只提供檢視，切換到編輯時先跳確認。
- **狀態保存**：`localStorage` 記住已開分頁、當前分頁、主題、字級、資料夾展開狀態，
  重新整理後回到原本的工作狀態。
- **鍵盤**：`Ctrl+S` 存檔、`Ctrl+W` 關分頁、`Ctrl+F` 聚焦搜尋框。

## 七、目前規模（建置時實測）

11 個資料夾、164 個 HTML、約 25 MB。最大兩檔為 `奧德賽(荷馬).html`（9.0 MB）與
`伊利亞德(荷馬).html`（8.6 MB）。

## 八、與既有兩套系統的關係

這是**第三套獨立的建置產物**，不要與前兩套混用：

| 系統 | 來源 | 建置腳本 | 產出 |
|---|---|---|---|
| 電子書庫 | `epub\` | `tools\build_library.py` | `reader\index.html` |
| 知識地圖首頁 | `html\` + 各 `order.md` | `tools\build_html_index.py` | `index.html` |
| **檔案樹瀏覽器** | `html\` | `tools\build_treeview.py` | `treeview.html` |

`index.html` 是給讀者看的策展入口（只列 `order.md` 挑選過的條目）；
`treeview.html` 是給自己用的維護工具（列出全部檔案，可編輯）。兩者用途不同，並存。
